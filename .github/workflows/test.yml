on: [push, pull_request]
name: Continuous integration
jobs:
  test:
    name: run tests and linters
    uses: viccuad/github-actions/.github/workflows/reusable-test-policy-rust.yml@v1
    with:
      artifacthub: true
